# Skatinov LeadLab

Серьёзный FastAPI-проект для лидогенерации.

---

## 🧱 Черновая структура проекта

На данном этапе проект устроен так:

```
Skatinov-LeadLab
├── app/                 # Backend-код приложения
│   ├── api/             # Маршруты (эндпоинты) API
│   ├── core/            # Ядро и базовые настройки
│   ├── models/          # Модели данных / БД
│   ├── schemas/         # Схемы данных (валидация)
│   ├── services/        # Бизнес-логика (сервисы)
│   ├── db/              # Подключение и работа с БД
│   └── config/          # Конфигурация приложения
├── tests/               # Автоматические тесты
└── frontend/            # Заготовка под будущий UI
```

---

## ⚙️ Конфигурация и окружения

Проект поддерживает три окружения: **dev** (разработка), **test** (тестирование) и **prod** (продакшен).

### 📁 Структура конфигурации
```
Skatinov-LeadLab
├── app/
│   └── config/
│   ├── init.py
│   └── settings.py # 🔧 Главный класс конфигурации (Pydantic Settings)
├──.env.dev.example # 📄 Шаблон для локальной разработки
├──.env.test.example # 📄 Шаблон для тестов
├──.env.prod.example # 📄 Шаблон для продакшена
├──.env.dev # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
├──.env.test # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
└──.env.prod # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
```

---


### 🚀 Как использовать конфигурацию

**1. В коде приложения:**
```python
from app.config import settings

# Использование в коде
print(settings.database_url)
print(settings.debug)
print(settings.environment.value)
```
**2. Переключение между окружениями:**
```python
from app.config import get_settings

# Получить конфиг для конкретного окружения
prod_settings = get_settings("prod")
dev_settings = get_settings("dev")
```
**3. Создание .env файла для разработки:**

Скопируй `.env.dev.example` → `.env.dev`

Отредактируй значения под свою систему

`.env.dev` уже в `.gitignore` ✅

---

### 🔐 Переменные конфигурации

_____________________________________________________________________________________________________
| Переменная     | Dev               | Test            | Prod                  | Описание            |
| -------------- | ----------------- | --------------- | --------------------- | ------------------- |
| `ENVIRONMENT`  | `dev`             | `test`          | `prod`                | Текущее окружение   |
| `DEBUG`        | `true`            | `false`         | `false`               | Режим отладки       |
| `DATABASE_URL` | SQLite (локально) | SQLite (тест)   | PostgreSQL (удалённо) | URL БД              |
| `LOG_LEVEL`    | `DEBUG`           | `INFO`          | `WARNING`             | Уровень логирования |
| `SECRET_KEY`   | dev-only          | dev-only        | **🔒 СЕКРЕТНО**       | Секретный ключ      |
_____________________________________________________________________________________________________

---

### ⚠️ Безопасность
Никогда не коммить реальные .env файлы в Git!

Файлы `.env.dev`, `.env.test`, `.env.prod` содержат секреты 🔐

Они находятся в `.gitignore`

Используй `.env.*.example` как шаблоны

Каждый разработчик создаёт свои `.env.dev` из примера

---

### ✅ Проверка конфигурации
Запусти тестовый скрипт:
```bash
poetry run python test_config_all_envs.py
```
Покажет, какие настройки загружены для каждого окружения.

```bash
======================================================================
TESTIROVANIE VSEKH OKRUZHENII (dev, test, prod)
======================================================================

[1] OKRUZHENIE: DEV
----------------------------------------------------------------------
Okruzhenie:        dev
Imya:              Skatinov LeadLab
Debug:             True
BD:                sqlite:///./skatinov_dev.db
API nazvanie:      Skatinov LeadLab API (Dev)
Uroven logov:      DEBUG

[2] OKRUZHENIE: TEST
----------------------------------------------------------------------
Okruzhenie:        test
Imya:              Skatinov LeadLab
Debug:             False
BD:                sqlite:///./skatinov_test.db
API nazvanie:      Skatinov LeadLab API (Test)
Uroven logov:      INFO

[3] OKRUZHENIE: PROD
----------------------------------------------------------------------
Okruzhenie:        prod
Imya:              Skatinov LeadLab
Debug:             False
BD:                postgresql://user:password@db.example.com:5432/skatinov_prod
API nazvanie:      Skatinov LeadLab API
Uroven logov:      WARNING

======================================================================
[OK] Vse tri okruzheniia zagruzheny uspeshno!
======================================================================
```

---

### 🗄️ База данных и миграции

**📦 Подключение к базе данных**

Я настроил модуль для подключения к базе данных через SQLAlchemy.
```
Skatinov-LeadLab
├── app/
│   ├── db/
│   │   └── database.py   # Подключение к БД (engine, SessionLocal, Base)
│   └── models/
│       └── lead.py       # Модель лида (таблица leads)
```

В `app/db/database.py` я:

создал `engine` на основе `settings.database_url` (dev/test/prod берутся из конфигурации);
​

настроил `SessionLocal` — фабрику сессий для работы с БД в эндпоинтах;
​

описал базовый класс моделей `Base = declarative_base()`, от которого наследуются все таблицы.

**👤 Модель лида (Lead)**

Я описал модель лида `Lead` в `app/models/lead.py` — это основная таблица для потенциальных клиентов.
​

Основные поля:

- `name` — имя лида, чтобы менеджер понимал, как к нему обращаться.
​

- `email` — основной канал связи для отправки писем и уведомлений.
​

- `status` — статус лида (`new`, `in_progress`, `won`, `lost` и т.п.), помогает видеть, на каком этапе находится клиент.
​

- `source` — источник лида (сайт, реклама, рекомендации), нужен для аналитики маркетинга.
​

- `assigned_to` — ответственный менеджер, который ведёт этого лида.
​

- `created_at` / updated_at — временные метки создания и последнего изменения записи.
​

Для полей `name`, `email`, `status`, `source`, `assigned_to` и `id` включены индексы — это ускоряет поиск лидов по ключевым полям.

**🧬 Миграции Alembic**

Я настроил Alembic для управления схемой базы и связал его с конфигурацией проекта.
​

Структура:
```
Skatinov-LeadLab
├── alembic.ini              # Конфигурация Alembic с русскими комментариями
└── migrations/
    ├── env.py               # Связка Alembic с Settings и Base
    ├── script.py.mako       # Шаблон для файлов миграций
    └── versions/
        └── d88f5dc1c6e0_create_leads_table.py  # Первая миграция (таблица leads)
```
Что важно:

- В `alembic.ini` я оставил заглушку `sqlalchemy.url = sqlite:///./dummy.db`, а реальный URL базы подставляется из `app.config.settings.database_url` в `migrations/env.py`.
​

- В `migrations/env.py` Alembic использует `Base.metadata`, чтобы автоматически видеть все модели (`Lead` и будущие).
​

Первая миграция d88f5dc1c6e0_create_leads_table.py:

- создаёт таблицу `leads` со всеми полями модели `Lead`;

- создаёт индексы для быстрого поиска по `id`, `name`, `email`, `status`, `source`, `assigned_to`;

- умеет откатываться (функция `downgrade` удаляет индексы и таблицу).

**▶️ Как применить миграции локально**

1. Убедись, что `.env.dev` настроен и содержит корректный `DATABASE_URL` (по умолчанию — SQLite `sqlite:///./skatinov_dev.db`).
​

2. Запусти миграции:

```bash
poetry run alembic upgrade head
```
Это создаст (или обновит) таблицу `leads` и все индексы в dev‑базе.

---

### 🐳 Docker‑инфраструктура (Postgres, RabbitMQ, backend)
На этом этапе я добавил к проекту полноценный Docker‑стенд: база данных, брокер сообщений и backend‑контейнер собираются и запускаются одной командой. Это позволяет поднимать окружение разработчика максимально похоже на продакшен, не устанавливая Postgres и RabbitMQ напрямую на систему.
​

**📁 Новые файлы и структура:**

```
Skatinov-LeadLab
├── docker-compose.yml      # Docker-стенд: Postgres, RabbitMQ, backend
├── Dockerfile              # Образ backend-сервиса (FastAPI + Uvicorn)
├── .env                    # Локальные переменные для Docker (НЕ в Git)
└── .env.docker.example     # Шаблон переменных для Docker-стенда​
```


- `docker-compose.yml` описывает три сервиса:

- `postgres` — контейнер с `postgres:16`, томом `postgres_data`, healthcheck и пробросом порта `5432`.

- `rabbitmq` — контейнер `rabbitmq:3-management` с томом `rabbitmq_data`, healthcheck и панелью управления на `http://localhost:15672`.

- `backend` — контейнер `skatinov-leadlab-backend`, собираемый из `Dockerfile`, подключён к Postgres и RabbitMQ через переменные окружения (`DATABASE_URL`, `RABBITMQ_URL`).
​

- `Dockerfile` собирает минимальный образ backend‑сервиса на базе `python:3.12-slim`: устанавливает `fastapi` и `uvicorn`, копирует код в `/app` и запускает `uvicorn app.main:app` на порту `8000`.
​

- `.env.docker.example` — шаблон настроек для Docker‑стенда; на его основе локально создаётся `.env` в корне.
  
---

### 🔐 Переменные окружения для Docker

Все логины/пароли и хосты для Docker‑сервисов вынесены в `.env` (файл лежит рядом с `docker-compose.yml` и не коммитится).
​

Пример содержимого:
```
# Настройки Postgres для Docker-стенда
POSTGRES_USER=skatinov_user
POSTGRES_PASSWORD=skatinov_pass123
POSTGRES_DB=skatinov_leadlab
POSTGRES_PORT=5432

# Настройки RabbitMQ для Docker-стенда
RABBITMQ_USER=skatinov_rmq
RABBITMQ_PASSWORD=skatinov_rmqpass123
RABBITMQ_PORT=5672
RABBITMQ_ADMIN_PORT=15672
```

Файл `.env` добавлен в `.gitignore` вместе с `.env.dev`, `.env.test`, `.env.prod` и `.env.docker`, а в репозиторий попадает только `.env.docker.example` как справочник по нужным переменным.
  
---

### 🚀 Запуск Docker‑стенда
После настройки `.env` весь стенд поднимается одной командой из корня проекта:

```bash
# Сборка backend-образа и запуск всех контейнеров
docker compose up -d --build
```

В результате доступны:

- Postgres: `localhost:5432` (используется Alembic и backend‑сервисом).

- RabbitMQ:

   - AMQP: `amqp://<RABBITMQ_USER>:<RABBITMQ_PASSWORD>@localhost:5672/`,

   - панель управления: `http://localhost:15672` (логин/пароль из `.env`).

- Backend:

   - HTTP API: `http://localhost:8000`,

   - health‑проверка-заглушка: `http://localhost:8000/health` возвращает JSON `{"status": "ok", "service": "backend", "env": "docker"}`.
  
---

### ​🧩 Почему это похоже на prod

- Каждый компонент (бэкенд, база, брокер) запущен в отдельном контейнере и подключён к общей сети `leadlab_network`, как в типичном микросервисном окружении.
​

- Данные Postgres и RabbitMQ не теряются при пересборке backend‑образа thanks to именованные тома `postgres_data` и `rabbitmq_data`, которые Docker хранит отдельно от контейнеров.
​

- Настройки вынесены в переменные окружения, поэтому для будущих dev/test/prod‑профилей можно будет использовать ту же схему, просто меняя `.env` и не трогая код приложения.
  
---

### ⚙️ Каркас FastAPI (core, middleware, CORS, health)

На этом этапе я собрал основу backend‑приложения на FastAPI поверх уже готовой конфигурации, моделей, миграций и Docker‑стенда.
​

**📁 Структура backend‑каркаса**​
```
Skatinov-LeadLab
├── app/
│   ├── main.py          # Точка входа FastAPI-приложения (create_app)
│   ├── api/
│   │   ├── __init__.py  # Пакет для HTTP-эндпоинтов
│   │   └── v1/
│   │       ├── __init__.py  # Пакет API v1
│   │       ├── api.py       # Корневой роутер /api/v1/*
│   │       └── health.py    # Базовый health-check сервиса
│   └── config/
│       └── settings.py  # Pydantic Settings (используется в create_app)
```


**🚀 Инициализация FastAPI‑приложения**

Я создал фабрику `create_app()` в `app/main.py`, которая отвечает за сборку всего приложения: конфиг, middleware и подключение версионированных роутеров.


```python
# app/main.py

import time
import logging

from fastapi import FastAPI, Request
from fastapi.responses import JSONResponse
from fastapi.middleware.cors import CORSMiddleware

from app.api.v1 import api_router as api_v1_router
from app.config.settings import settings  # Pydantic Settings


logger = logging.getLogger("skatinov_leadlab")


def create_app() -> FastAPI:
    """
    Фабрика приложения FastAPI.

    Здесь я:
    - подтягиваю конфигурацию из settings,
    - регистрирую middleware (логирование, CORS),
    - подключаю версионированные роутеры /api/v1/*.
    """
    app = FastAPI(
        title="Skatinov LeadLab",
        version=getattr(settings, "APP_VERSION", "0.1.0"),
        description=getattr(
            settings,
            "APP_DESCRIPTION",
            "Серьёзный FastAPI‑проект для лидогенерации.",
        ),
        debug=getattr(settings, "DEBUG", True),
    )

    # ---------- Middleware логирования ----------

    @app.middleware("http")
    async def log_requests(request: Request, call_next):
        """
        Простое HTTP‑middleware для логирования.

        Я логирую:
        - метод и путь запроса,
        - код ответа,
        - время обработки.

        Тела запросов и ответов не читаю, чтобы не светить секреты и не замедлять обработку.
        """
        start_time = time.time()
        try:
            response = await call_next(request)
        except Exception as exc:
            process_time = time.time() - start_time
            logger.exception(
                "Unhandled error: %s %s (%.3f s)",
                request.method,
                request.url.path,
                process_time,
            )
            return JSONResponse(
                status_code=500,
                content={"detail": "Internal server error"},
            )

        process_time = time.time() - start_time
        logger.info(
            "%s %s -> %s (%.3f s)",
            request.method,
            request.url.path,
            response.status_code,
            process_time,
        )
        return response

    # ---------- CORS для фронтенда ----------

    allowed_origins = [
        "http://localhost:3000",
        "http://127.0.0.1:3000",
    ]

    app.add_middleware(
        CORSMiddleware,
        allow_origins=allowed_origins,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    # ---------- Роутеры ----------

    # Подключаю роутер версии API v1 с префиксом /api/v1
    app.include_router(api_v1_router, prefix="/api/v1")

    return app


# Экземпляр приложения, который будет запускать Uvicorn
app = create_app()
```


**📡 Версионирование API и health‑check**

Я ввёл явный слой версии `v1`: общий роутер лежит в `app/api/v1/api.py`, а базовый health‑чек вынесен в отдельный модуль `health.py`.


```python
# app/api/v1/api.py

from fastapi import APIRouter

from .health import router as health_router

api_router = APIRouter()

# Подключаю модуль health под /health
api_router.include_router(health_router, prefix="/health", tags=["health"])
```


```python
# app/api/v1/health.py

from fastapi import APIRouter

router = APIRouter()


@router.get("", summary="Базовый health-check сервиса")
async def health_root():
    """
    Простейший health‑эндпоинт.

    Здесь я проверяю только то, что приложение запущено
    и отвечает на HTTP‑запросы. Позже добавлю проверки БД и брокера.
    """
    return {
        "status": "ok",
        "service": "skatinov-leadlab",
        "details": "base app is up",
    }
```



В результате из Docker‑стенда доступен эндпоинт:

- `GET http://localhost:8000/api/v1/health` → `{"status": "ok", "service": "skatinov-leadlab", "details": "base app is up"}`.
​

**🐳 Docker: обновлённый образ backend**

Я доработал `Dockerfile`, чтобы backend‑контейнер собирался с нужными библиотеками (FastAPI, Uvicorn, Pydantic Settings, SQLAlchemy, Alembic) и запускал именно `app.main:app`.

```
# Базовый Dockerfile для backend Skatinov LeadLab
FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
 && rm -rf /var/lib/apt/lists/*

# Устанавливаю зависимости проекта напрямую через pip
RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    "pydantic-settings>=2.12.0,<3.0.0" \
    "python-dotenv>=1.2.1,<2.0.0" \
    "sqlalchemy>=2.0.45,<3.0.0" \
    "alembic>=1.17.2,<2.0.0"

# Копирую весь проект внутрь контейнера
COPY . /app

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```


Backend теперь собирается и запускается одной командой:
```
docker compose up --build
```


и сразу отдаёт `200 OK` на `/api/v1/health` из контейнера.
