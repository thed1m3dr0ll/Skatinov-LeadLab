# Skatinov LeadLab

Серьёзный FastAPI-проект для лидогенерации.

---

## 🧱 Черновая структура проекта

На данном этапе проект устроен так:

```txt
Skatinov-LeadLab
├── app/                 # Backend-код приложения
│   ├── api/             # Маршруты (эндпоинты) API
│   ├── core/            # Ядро и базовые настройки
│   ├── models/          # Модели данных / БД
│   ├── schemas/         # Схемы данных (валидация)
│   ├── services/        # Бизнес-логика (сервисы)
│   ├── db/              # Подключение и работа с БД
│   └── config/          # Конфигурация приложения
├── tests/               # Автоматические тесты
└── frontend/            # Заготовка под будущий UI
```

---

## ⚙️ Конфигурация и окружения

Проект поддерживает три окружения: **dev** (разработка), **test** (тестирование) и **prod** (продакшен).

### 📁 Структура конфигурации
```txt
Skatinov-LeadLab
├── app/
│   └── config/
│   ├── init.py
│   └── settings.py # 🔧 Главный класс конфигурации (Pydantic Settings)
├──.env.dev.example # 📄 Шаблон для локальной разработки
├──.env.test.example # 📄 Шаблон для тестов
├──.env.prod.example # 📄 Шаблон для продакшена
├──.env.dev # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
├──.env.test # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
└──.env.prod # 🔐 РЕАЛЬНЫЙ файл (не коммитится в Git!)
```

---


### 🚀 Как использовать конфигурацию

**1. В коде приложения:**
```python
from app.config import settings

# Использование в коде
print(settings.database_url)
print(settings.debug)
print(settings.environment.value)
```
**2. Переключение между окружениями:**
```python
from app.config import get_settings

# Получить конфиг для конкретного окружения
prod_settings = get_settings("prod")
dev_settings = get_settings("dev")
```
**3. Создание .env файла для разработки:**

Скопируй `.env.dev.example` → `.env.dev`

Отредактируй значения под свою систему

`.env.dev` уже в `.gitignore` ✅

---

### 🔐 Переменные конфигурации

_____________________________________________________________________________________________________
| Переменная     | Dev               | Test            | Prod                  | Описание            |
| -------------- | ----------------- | --------------- | --------------------- | ------------------- |
| `ENVIRONMENT`  | `dev`             | `test`          | `prod`                | Текущее окружение   |
| `DEBUG`        | `true`            | `false`         | `false`               | Режим отладки       |
| `DATABASE_URL` | SQLite (локально) | SQLite (тест)   | PostgreSQL (удалённо) | URL БД              |
| `LOG_LEVEL`    | `DEBUG`           | `INFO`          | `WARNING`             | Уровень логирования |
| `SECRET_KEY`   | dev-only          | dev-only        | **🔒 СЕКРЕТНО**       | Секретный ключ      |
_____________________________________________________________________________________________________

---

### ⚠️ Безопасность
Никогда не коммить реальные .env файлы в Git!

Файлы `.env.dev`, `.env.test`, `.env.prod` содержат секреты 🔐

Они находятся в `.gitignore`

Используй `.env.*.example` как шаблоны

Каждый разработчик создаёт свои `.env.dev` из примера

---

### ✅ Проверка конфигурации
Запусти тестовый скрипт:
```bash
poetry run python test_config_all_envs.py
```
Покажет, какие настройки загружены для каждого окружения.

```bash
======================================================================
TESTIROVANIE VSEKH OKRUZHENII (dev, test, prod)
======================================================================

[1] OKRUZHENIE: DEV
----------------------------------------------------------------------
Okruzhenie:        dev
Imya:              Skatinov LeadLab
Debug:             True
BD:                sqlite:///./skatinov_dev.db
API nazvanie:      Skatinov LeadLab API (Dev)
Uroven logov:      DEBUG

[2] OKRUZHENIE: TEST
----------------------------------------------------------------------
Okruzhenie:        test
Imya:              Skatinov LeadLab
Debug:             False
BD:                sqlite:///./skatinov_test.db
API nazvanie:      Skatinov LeadLab API (Test)
Uroven logov:      INFO

[3] OKRUZHENIE: PROD
----------------------------------------------------------------------
Okruzhenie:        prod
Imya:              Skatinov LeadLab
Debug:             False
BD:                postgresql://user:password@db.example.com:5432/skatinov_prod
API nazvanie:      Skatinov LeadLab API
Uroven logov:      WARNING

======================================================================
[OK] Vse tri okruzheniia zagruzheny uspeshno!
======================================================================
```
